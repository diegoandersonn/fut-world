export default class CreateTeam {
    constructor(name, country, stadium, logo, id) {
      this.name = name;
      this.country = country;
      this.stadium = stadium;
      this.logo = logo;
      this.id = id
    }
  }